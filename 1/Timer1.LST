C51 COMPILER V9.54   TIMER1                                                                09/14/2026 20:49:16 PAGE 1   


C51 COMPILER V9.54, COMPILATION OF MODULE TIMER1
OBJECT MODULE PLACED IN Timer1.OBJ
COMPILER INVOKED BY: D:\C51\BIN\C51.EXE Timer1.c OPTIMIZE(8,SPEED) BROWSE DEBUG OBJECTEXTEND TABS(2)

line level    source

*** WARNING C500 IN LINE 1 OF Timer1.c: MISSING LICENSE ID CODE (LIC) IN 'TOOLS.INI'
   1          #include "Timer1.h"
   2          
   3          unsigned char g_key = 0;
   4          
   5          void Timer1_Init(void) {
   6   1          TMOD |= 0x10;    // Timer1 mode 1 (16-bit)
   7   1          TH1 = 0xB1;      // 10ms @ 24MHz
   8   1          TL1 = 0xE0;
   9   1          ET1 = 1;         // enable Timer1 interrupt
  10   1          TR1 = 1;         // start Timer1
  11   1      }
  12          
  13          void Timer1_Routine(void) interrupt 3 {
  14   1          static unsigned char key_now = 0, key_prev = 0, key_locked = 0;
  15   1      
  16   1          TH1 = 0xB1;      // reload
  17   1          TL1 = 0xE0;
  18   1      
  19   1          key_now = 0;
  20   1          if (KEY_PLAY == 0) key_now = 1;   // pressed = low level
  21   1          if (KEY_NEXT == 0) key_now = 2;
  22   1          if (KEY_PREV == 0) key_now = 3;
  23   1          if (KEY_STOP == 0) key_now = 4;
  24   1      
  25   1          if (key_now == key_prev) {        // debounce: two samples equal
  26   2              if (!key_locked && key_now != 0) {
  27   3                  g_key = key_now;          // edge trigger once
  28   3                  key_locked = 1;
  29   3              }
  30   2              if (key_locked && key_now == 0) key_locked = 0;
  31   2          }
  32   1          key_prev = key_now;
  33   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =     87    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =      4    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  1 WARNING(S),  0 ERROR(S)
